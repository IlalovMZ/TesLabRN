import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  modal: {
    margin: 0,
  },
  container: {
    flexShrink: 1,
    backgroundColor: '#fff',
    borderRadius: 10,
    overflow: 'hidden',
  },
})
