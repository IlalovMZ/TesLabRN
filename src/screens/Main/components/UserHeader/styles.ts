import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  userBlock: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 16,
    backgroundColor: '#CCCCCC',
    borderRadius: 20,
    marginBottom: 16,
  },
  info: {
    marginLeft: 16,
  },
})
